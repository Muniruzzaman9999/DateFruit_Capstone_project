# Backend dependencies for the Date Fruit Marketplace project.
#
# These are the versions actually installed and verified on:
#   Windows 11 Pro / Python 3.13.3 / CPU-only (no NVIDIA GPU, no CUDA)
#
# Install with:
#   .venv\Scripts\python.exe -m pip install -r backend\requirements.txt

# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.52.3
python-multipart==0.0.32          # required by FastAPI to receive uploaded files

# --- Database ---
SQLAlchemy==2.0.52
alembic==1.19.1
psycopg[binary]==3.3.4            # PostgreSQL driver (psycopg 3, prebuilt binary wheel)

# --- Configuration & validation ---
pydantic==2.13.4
pydantic-settings==2.15.0         # reads settings from the .env file
email-validator==2.3.0            # required by pydantic's EmailStr type

# --- Authentication ---
PyJWT==2.13.0                     # create/verify JWT access tokens
bcrypt==5.0.0                     # password hashing

# --- Machine learning ---
# On Windows the plain `tensorflow` wheel is CPU-only, which is exactly what
# this project needs. Do NOT replace this with a GPU/CUDA build.
tensorflow==2.21.0
Pillow==12.3.0                    # opening and resizing uploaded images

# --- Tests ---
pytest==9.1.1
httpx==0.28.1                     # used by FastAPI's TestClient
